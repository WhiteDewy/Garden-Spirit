# ============================================================
# 星灵花园前端环境配置模板
# 复制为 .env.development / .env.production 并按环境填写。
# 构建/开发时由 vite 注入，勿写死在前端代码里。
# ============================================================

# 后端 API 地址（FastAPI）
# 开发：本地 uvicorn（默认 http://127.0.0.1:8756）
# 生产：必须填真实域名（HTTPS），例如 https://api.your-domain.com
VITE_API_BASE=http://127.0.0.1:8756
