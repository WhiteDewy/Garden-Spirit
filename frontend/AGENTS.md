# frontend 开发约束

1. **先读 `docs/frontend_ui_conventions.md`**：按钮同高居中、弹层跟随场景明暗、场景背景无断层等规范是硬约束。
2. `src/pages/universe/wheel.vue` 视觉冻结，不改样式。
3. 视觉方向以 `docs/frontend_visual_refactor_spec.md`（星灵森屿）为准；功能接入清单以 `docs/frontend_backlog.md` 为准。
4. 后端 API 形状见 `src/api/client.ts`；未经确认不改后端代码。
